services:
  - type: web
    name: funfillers-backend
    env: go
    buildCommand: go build -o server main.go
    startCommand: ./server
    envVars:
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production
